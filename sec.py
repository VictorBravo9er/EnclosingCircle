"""Attempt to make closest enclosing Circle."""
from process import ProcessCircle


class sec(ProcessCircle):
    """Class for Smallest enclosing circle."""

    def __init__(self) -> None:
        """Initialize method."""
        super().__init__()

    @staticmethod
    def comparison(lst):
        """Compare closest point."""
        idx = 0
        for i in range(3):
            pass
        # idiot's dream.

    @staticmethod
    def process(data):
        """Process data."""
        processor = []
        for point in super().reader(data):
            pass
